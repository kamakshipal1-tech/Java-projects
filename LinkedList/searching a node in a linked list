boolean search(Node head, Node node) {

    if (head == null) return false;

    Node temp = head;

    while (temp != null) {
        if (temp.val == node) {
            return true;
        }
        temp = temp.next;
    }

    return false;
}
